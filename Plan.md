# Plan.md — рабочая приборная панель проекта Арина

## Правило поддержки актуальности плана (обязательно к исполнению)

После завершения каждой фазы, до начала следующей:
1. Отметить все задачи завершённой фазы статусом `✅ Готово` в таблице задач этой фазы.
2. Обновить строку фазы в «Таблице прогресса» ниже: **Факт** — что реально сделано и
   чем отличается от плана (если отличается), **Статус** → `✅`, **Блокеры** → очистить.
3. Отклонение от плана — фиксировать в столбце **Факт**, не переписывая **План** —
   так сохраняется история решений.
4. Заблокированная фаза — статус `🔴 Заблокировано`, в **Блокеры** — что мешает и что
   нужно, чтобы снять блокировку.
5. Полный технический план с архитектурными решениями и обоснованиями —
   `thoughts/shared/plans/2026-08-19-arina-mvp-implementation-plan.md` — не переписывать
   задним числом, только дополнять.

Источники: идея — `idea.md`, исследование — `research.md`, критика и фактчекинг —
`критика.md`, спецификация — `thoughts/shared/specs/2026-08-19-arina-personal-agent.md`,
технический план — `thoughts/shared/plans/2026-08-19-arina-mvp-implementation-plan.md`.

## Блокер верхнего уровня (снят полностью)

Изначально здесь был зафиксирован блокер: разработка Фазы 3 и всего, что от неё
зависит (классификация, задачи, напоминания, summary диалога, документы — фактически
весь продукт, кроме каркаса), заблокирована до тех пор, пока в соседнем проекте
**Aiy** не стабилизирован в проде релей OpenRouter через Cloudflare Worker. До снятия
блокера допускалась только работа над Фазами 0-2.

**Снят для разработки 2026-08-19.** Решение: Арина подключается к OpenRouter напрямую,
собственным API-ключом, без релея и без зависимости от инфраструктуры Aiy. Причина —
независимо проверено (из другой сессии), что `https://openrouter.ai/api/v1/models`
отвечает HTTP 200 напрямую с локального компьютера разработчика, без прокси. Фаза 3
разблокирована для разработки и тестирования — и это подтверждение остаётся верным,
локальная работа весь день шла без проблем.

**Уточнение 2026-08-20, при живом деплое на Beget (см. Фазу 9)**: проверка была
неполной — сделана только с локальной машины, не с реального прод-сервера. С IP
Beget VPS (Россия) `openrouter.ai` отвечает `403 Access denied by security policy`
на любой запрос. Для прод-окружения релей всё же нужен — не из Aiy (проект остаётся
полностью независимым от её инфраструктуры, см. правило ниже), а свой, отдельный.
Собственный Cloudflare Worker-релей не сработал по архитектурной причине (Cloudflare
не даёт обходить через Workers блокировки на других Cloudflare-зонах).

**Решено 2026-08-20.** Свой собственный релей на платформе вне Cloudflare — минимальный
прозрачный HTTP-прокси на чистом Python (`urllib`/`http.server`, без зависимостей),
репозиторий `elenakolodnitskay-web/arina-openrouter-relay` (публичный — приватные
репозитории оказались недоступны для аккаунта из-за экспортных ограничений GitHub,
так же как раньше с основным репозиторием Арины), задеплоен на Render.com (Oregon,
US West — не Cloudflare, не Россия) через `render.yaml` (Blueprint). Проверено с
самого Beget-сервера: `curl` на релей → `200`, реальный вызов `llm/client.complete()`
изнутри прод-контейнера → успешный ответ модели. `OPENROUTER_BASE_URL` в проде
переключён на `https://arina-openrouter-relay.onrender.com/api/v1`, контейнер бота
пересоздан. Ни в коде, ни в инфраструктуре релея нет ничего от Aiy — отдельный
GitHub-репозиторий, отдельный аккаунт Render, ключ OpenRouter передаётся насквозь
через заголовок `Authorization` от самой Арины, в релее не хранится.
Локальная разработка блокером не затронута.

## Таблица прогресса

| Фаза | План | Факт | Статус | Блокеры |
|---|---|---|---|---|
| 0. Окружение и конвенции | Инициализировать репозиторий, `venv/`, `.gitignore`, `config.py` (pydantic-settings), `requirements.txt`, скелет структуры каталогов из `CLAUDE.md` | Сделано по плану: `git init`, `venv/`, `.gitignore` (venv/, __pycache__/, *.pyc, .env, *.db, *.sqlite3, кеши pytest/mypy), `requirements.txt` (стек из CLAUDE.md), `config.py` на pydantic-settings (`TELEGRAM_BOT_TOKEN`, `DATABASE_URL`, `OPENROUTER_BASE_URL`, `OPENROUTER_API_KEY`, `FERNET_KEY`, `ALLOWED_USER_IDS`), скелет каталогов `db/`, `llm/`, `core/`, `bot/handlers/`, `tests/calibration/` с `__init__.py`, `.env.example`, `tests/test_config.py` (3 теста, все зелёные) | ✅ | — |
| 1. Хранилище данных | `db/models.py` (User, Task, Note, DialogSummary), `db/crypto.py` (Fernet), `db/session.py`, Alembic-миграции, PostgreSQL локально | Сделано: `db/models.py` (User/Task/Note/DialogSummary, `context`/`status` как `Enum(..., native_enum=False)` — портируемо между Postgres и SQLite), `db/crypto.py` (`EncryptedString` — TypeDecorator, прозрачное Fernet-шифрование `profile_summary`/`Task.title`/`Note.content`/`DialogSummary.summary_text`), `db/session.py` (фабрика сессий из `settings.database_url`), Alembic настроен на `config.settings` и `Base.metadata`, начальная миграция автосгенерирована и вручную дополнена импортом `db.crypto`. Тесты: `tests/test_crypto.py`, `tests/test_models.py` (round-trip шифрования на `sqlite://` in-memory) — все зелёные. **Отклонение от плана**: в этом окружении нет локального PostgreSQL/Docker — `alembic upgrade head` реально проверен на SQLite (файл `arina_dev.db`, создан и удалён), а не на «чистой PostgreSQL» как в критерии готовности. Модели используют только портируемые типы, миграция должна быть перепроверена на настоящем PostgreSQL перед Фазой 9 (деплой). Также при попутной правке обнаружена и исправлена утечка состояния между тестами: `tests/test_config.py` мутировал `os.environ` на уровне модуля — переписан на `monkeypatch`. Docker Desktop 4.87.0 установлен через winget (2026-08-19). WSL2 включена (пользователь поставил вручную через `wsl --install` от администратора и перезагрузился — автоматическое повышение прав через `Start-Process -Verb RunAs` не сработало неинтерактивно, потребовалось сделать руками). Итог 2026-08-19: `docker compose up -d` поднял `postgres:16-alpine` (healthcheck зелёный), `DATABASE_URL` в `.env` переключён на `postgresql://arina:arina@localhost:5432/arina`, `alembic upgrade head` создал все 5 таблиц (`users`, `tasks`, `notes`, `dialog_summaries`, `alembic_version`) на настоящем PostgreSQL, вручную проверен round-trip шифрования через ORM (запись/чтение `Task.title`, `raw` — Fernet-шифротекст, расшифровка совпадает) — критерий готовности выполнен буквально. Попутно найдена и исправлена вторая утечка ambient-состояния: `test_allowed_user_ids_list_empty_by_default` неявно полагался на пустой `ALLOWED_USER_IDS` в `.env`, сломался после того как туда вписали реальные ID для Фазы 2 — переписан на явный `Settings(allowed_user_ids="")`. | ✅ | — |
| 2. Каркас Telegram-бота | `bot/main.py`, приём `/start`, онбординг-опрос (`bot/states.py`, `bot/handlers/onboarding.py`), whitelist приглашённых пользователей, идентификация по Telegram ID | Сделано: `bot/states.py` (`OnboardingState`), `bot/handlers/onboarding.py` (`/start` → whitelist-проверка по `ALLOWED_USER_IDS` → короткий опрос → `receive_profile` пишет `User.profile_summary` зашифрованным и ставит `onboarding_completed=True`; повторный `/start` уже онбордившегося — приветствие без повторного опроса), `bot/main.py` (`Application`, `ConversationHandler` на онбординг + `CommandHandler("delete_my_data", ...)`). `/delete_my_data` реализован сразу как в плане — каскадно удаляет Task/Note/DialogSummary и саму строку User. Тесты: `tests/test_onboarding.py` (7 тестов: отказ не-whitelisted, приветствие нового и вернувшегося пользователя, шифрование `profile_summary` на диске, удаление данных для существующего/несуществующего пользователя) — через мок `Update`/`SessionLocal` на SQLite in-memory, без реального Telegram. `python -m bot.main` собирает `Application` с плейсхолдер-токеном без сетевых обращений. Живая проверка пройдена 2026-08-19: реальный токен от @BotFather (`@arina_bf_bot`) получен, вписан в `.env`, бот поднят локально (`python -m bot.main`), реальный пользователь (Telegram ID из `ALLOWED_USER_IDS`) прошёл `/start` → онбординг-вопрос → ответил текстом → получил «Профиль сохранён»; в БД (`arina_dev.db`) строка `User` с `onboarding_completed=True`, `profile_summary` расшифровывается ORM-слоем в исходный текст, а сырое значение колонки — Fernet-шифротекст (`gAAAAAB...`), не открытый текст. Второй ID из whitelist ещё не проходил онбординг на момент проверки — это не блокирует критерий, один успешный проход подтверждает работоспособность. Попутно найдено и объяснено (не баг): `venv`-лаунчер `python.exe` на этой машине запускает системный `python.exe` как дочерний процесс с тем же argv — выглядит как дублирующийся поллер, но функционально безвреден (один процесс делает всю работу). **Баг найден и исправлен 2026-08-19 (параллельная сессия/вкладка, при живой проверке Фазы 3-7)**: онбординг падал для второго тестового Telegram ID (`8866671232`, за пределами диапазона `int4`, ±2^31-1 ≈ 2.147 млрд) — `psycopg2.errors.NumericValueOutOfRange`, `User.telegram_id` был обычным `Integer`, а не `BigInteger`. Настоящие Telegram user ID давно превышают этот диапазон (Telegram — не Twitter/старые системы с 32-битными ID). Исправлено: `db/models.py` → `telegram_id: Mapped[int] = mapped_column(BigInteger, ...)`, миграция `05a354edee66_telegram_id_as_biginteger.py` (`alembic upgrade head` применена и проверена на настоящем PostgreSQL — `users.telegram_id` теперь `bigint`); при автогенерации миграции Alembic заодно предложил удалить `apscheduler_jobs` (управляется рантаймом APScheduler, не входит в наши модели) — вручную вычищено из миграции, не тронуто. Заодно добавлен `application.add_error_handler(_on_error)` в `bot/main.py` — необработанные исключения больше не приводят к тишине для пользователя (ответ «Что-то пошло не так на нашей стороне» + логирование трейсбека без пользовательских данных). Регрессия закрыта тестом `tests/test_postgres_regression.py` — юнит-тесты на SQLite этот класс багов не ловят (SQLite не проверяет ширину `INTEGER`), тест пропускается, если движок не PostgreSQL, и реально прогнан на настоящем Postgres — зелёный. Проверено, что похожих мест (Telegram ID в других колонках) в схеме нет. **Пробел найден и исправлен 2026-08-20 (живое использование реальным клиентом)**: нигде не объяснялось, что умеет Арина — список команд (`/start`, `/delete_my_data`, `/task`, `/tasks`, `/document`) существовал, но не было ни одной команды, которая бы их перечисляла пользователю. Не входило ни в один пронумерованный пункт плана — небольшое дополнение к Фазе 2 (`bot/handlers/onboarding.py`), не отдельная фаза. Добавлено: `HELP_TEXT` — статический текст на русском (свободный чат с автоклассификацией и кнопками коррекции, задачи/напоминания естественным языком + `/tasks`, `/document` с обязательным подтверждением, память о разговоре, `/delete_my_data`); `help_command()` — новый хендлер `/help` (whitelist-проверка как у остальных команд); в сообщение после онбординга добавлена строка «Если что — напишите /help, расскажу подробнее, что умею». Тесты: +2 в `tests/test_onboarding.py` (текст справки для разрешённого пользователя, отказ не-whitelisted), +1 проверка подсказки после сохранения профиля. | ✅ | — |
| 3. LLM-ядро | `llm/client.py` (OpenRouter через релей, retry), `llm/classify.py` (работа/личное), калибровочный набор диалогов, `tests/calibration/` | **Отклонение от плана**: релей OpenRouter через Cloudflare Worker (Aiy) не используется — 2026-08-19 принято решение подключаться к OpenRouter напрямую, собственным API-ключом. Причина: релей технически не нужен — независимо проверено (из другой сессии), что `https://openrouter.ai/api/v1/models` отвечает HTTP 200 напрямую с этой машины, без прокси, геоблокировки нет. `llm/client.py` использует `base_url=https://openrouter.ai/api/v1` вместо адреса Cloudflare Worker. Блокер верхнего уровня снят (см. раздел выше). Сделано: `llm/client.py` (`complete()` — обёртка над `openai.AsyncOpenAI`, retry с экспоненциальной паузой на `APIConnectionError`/`APITimeoutError`, `LLMUnavailableError` с понятным русским текстом после исчерпания попыток), `llm/classify.py` (`classify_message()` — промпт классификации на `Context.work`/`Context.personal` с уверенностью, разбор JSON устойчив к markdown-обёртке в ответе модели). Тесты: `tests/test_llm_client.py` (успех, ретрай-затем-успех, исчерпание попыток — на моках, без реальной сети), `tests/test_classify.py` (парсинг чистого и обёрнутого в markdown JSON, ошибка на нераспознаваемом ответе). `tests/calibration/dataset.py` — 10 сообщений (явно рабочие/личные + 2 неоднозначных, включая «созвон с Димой в 19» из спецификации), `tests/calibration/test_calibration.py` — пропускается по умолчанию (`RUN_CALIBRATION=1` для реального запуска), порог точности 0.7 временный, обсуждается по факту первых прогонов. Модель по умолчанию — `anthropic/claude-haiku-4.5` (не зафиксирована в исходных документах проекта, выбрана как быстрая/дешёвая для задачи бинарной классификации — решение этой сессии, может быть пересмотрено). **Живая проверка пройдена 2026-08-19**: настоящий `OPENROUTER_API_KEY` получен и работает; `complete()` отвечает на прямой вызов; `RUN_CALIBRATION=1 pytest tests/calibration` на реальной модели — **80% точности (8/10)**, порог 0.7 пройден, оба промаха — специально заложенные неоднозначные кейсы («созвон с Димой в 19», «встреча в кафе в субботу»), т.е. классификатор ошибается ровно там, где и должен быть неуверен. Критерий готовности выполнен. **Находка 2026-08-20 (боевой деплой на Beget)**: решение «релей не нужен» от 2026-08-19 оказалось неполным — проверка `HTTP 200` была сделана только с локального компьютера разработчика, не с реального прод-сервера. При живом деплое на Beget VPS (`159.194.214.72`, Рязанская область — см. Фазу 9) `openrouter.ai` отвечает `403 Access denied by security policy` на **любой** запрос с этого IP, включая неавторизованный `GET /api/v1/models` — похоже на гео-блокировку по IP/ASN, не проблема с ключом (`curl` подтверждает: `ipinfo.io` показывает `country: RU`, `org: Beget LLC`). Локальная разработка и все прежние живые проверки в этой сессии (Фазы 3-7) шли с локального компьютера — они не пострадали и остаются валидными для кода/тестов, но не доказывают работоспособность в проде. Релей всё же нужен — ровно то, для чего он изначально был у Aiy, просто не была проверена реальная прод-среда до самого деплоя. **Попытка 1 (не сработала)**: собственный Cloudflare Worker-релей (`arina-openrouter-relay`, отдельный от Aiy, свой аккаунт Cloudflare) — тоже получает 403 с Beget, даже после того как в коде Worker'а оставлены только безопасные заголовки (`Authorization`, `Content-Type`, без `CF-Connecting-IP`/`X-Forwarded-For`). Причина — не в заголовках: `openrouter.ai` и Cloudflare Workers оба на инфраструктуре Cloudflare, и Cloudflare архитектурно не даёt использовать Workers для обхода блокировок на других Cloudflare-зонах (иначе это дыra в их же защите) — реальный IP просачивается на внутреннем уровне сети, не через HTTP-заголовки. Подтверждено экспериментально: тот же Worker с локального компьютера — `200 OK`, с Beget — `403`, разница только в источнике запроса к Worker. **Решено 2026-08-20** — свой релей на Render.com (не Cloudflare), подробности в разделе «Блокер верхнего уровня» выше. Критерий готовности снова выполнен, теперь и для прод-окружения. | ✅ | — |
| 4. Свободный чат + классификация | `bot/handlers/free_chat.py` — приём сообщений, классификация, инлайн-кнопка коррекции классификации | Сделано: `bot/handlers/free_chat.py` (`handle_message` — whitelist → проверка онбординга → `classify_message` → сохранение `Note` с проставленным `context`, ответ с инлайн-кнопкой «Это не туда»; `handle_context_correction` — по нажатию находит `Note`, проверяет что она принадлежит нажавшему (сверка `telegram_id`), переключает `context` на противоположный, подтверждает текстом). Зарегистрировано в `bot/main.py`: `MessageHandler` для свободного текста после хендлеров онбординга/`delete_my_data`, `CallbackQueryHandler` на `^toggle_context:`. Тесты: `tests/test_free_chat.py` (6 тестов — игнор не-whitelisted, подсказка пройти `/start`, сохранение классифицированной заметки, обработка `LLMUnavailableError`, коррекция контекста, отказ поправить чужую заметку). **Отклонение от критерия готовности**: «исправление видно в БД» проверено на SQLite in-memory через мок `Update`/`SessionLocal`, не в реальном Telegram — живая проверка ждёт того же `OPENROUTER_API_KEY`, что и Фаза 3 (классификация реальных сообщений нужна для сквозного теста). **UX-баг найден и исправлен 2026-08-19 (параллельная сессия/вкладка, при живой проверке)**: переключатель «Это не туда» (`toggle_context:{note_id}`) при нажатии стирал содержательный ответ бота из Фазы 6.5 — `edit_message_text` заменял весь текст на голое «Исправил на «рабочее».», хотя Фаза 6.5 сделана позже Фазы 4 и этот конфликт заранее не был виден. Исправлено: кнопка-переключатель заменена на две явные кнопки «Рабочее»/«Личное» (`callback_data = set_context:{note_id}:work` / `...:personal` — сразу видно оба варианта, не только текущее состояние), подтверждение теперь через `query.answer(...)` (всплывающее уведомление), исходный ответ бота не трогается. `bot/main.py`: паттерн `CallbackQueryHandler` обновлён на `^set_context:`. Тесты: `test_context_correction_sets_chosen_context_and_confirms` (заменил тест на переключение), `test_context_correction_rejects_foreign_note` обновлён под новый `callback_data`. Заодно в `tests/test_postgres_regression.py` (добавлен в предыдущем коммите) заменён захардкоженный реальный Telegram ID из `ALLOWED_USER_IDS` на синтетический `9_999_999_999` — тест падал на конфликте с настоящей строкой пользователя в общей dev-базе (`UniqueViolation`). Итого 58 passed + 1 skipped (калибровочный) в прогоне. **Живая проверка пройдена 2026-08-19**: `handle_message`/`handle_context_correction` вызваны напрямую с настоящей БД (Postgres) и настоящей моделью (только объект Telegram `Update` подделан) — сообщение классифицировано (`work`), сохранено как `Note`, кнопка «Личное» переключила `context` на `personal`, проверено чтением из БД. Критерий готовности «исправление видно в БД» выполнен на реальном стеке. **Пробел найден и исправлен 2026-08-19** (см. подробности в Фазе 5): `handle_message` не знал о задачах/напоминаниях вообще — весь свободный текст шёл только в `classify_message` → `Note`, из-за чего «Напомни в 20:56 позвонить маме» обычным сообщением не срабатывало (только через `/task`), хотя спецификация описывает естественный язык как основной способ постановки задач. Добавлено определение намерения (`llm/intent.py`) перед классификацией. | ✅ | — |
| 5. Задачи и напоминания | `core/tasks.py` (разбор естественного языка), `core/scheduler.py` (APScheduler), `bot/handlers/tasks_flow.py`, разовые + повторяющиеся напоминания | Сделано: `core/tasks.py` (`parse_task()` — LLM-разбор формулировки в title/due_at (UTC)/recurrence_rule; повтор — cron-выражение из 5 полей, разово — `DateTrigger` по `due_at`), `core/scheduler.py` (`AsyncIOScheduler` + `SQLAlchemyJobStore` на той же PostgreSQL — переживает перезапуск процесса за счёт персистентного job store, не самодельной логики; `send_reminder()` создаёт короткоживущий `telegram.Bot` и шлёт сообщение), `bot/handlers/tasks_flow.py` (`/task <текст>` — парсинг + классификация контекста + сохранение + постановка в планировщик; `/tasks` — список активных с инлайн-кнопкой «Отменить»; `handle_cancel_task` — отмена с проверкой владельца). Планировщик запускается через `Application.post_init` в `bot/main.py`. Тесты: `test_tasks.py`, `test_scheduler.py`, `test_tasks_flow.py` (17 тестов, все на моках). Дополнительно — живой smoke-тест `AsyncIOScheduler`+`SQLAlchemyJobStore` на настоящем PostgreSQL (не требует LLM-ключа): задание реально создалось и удалилось в таблице `apscheduler_jobs`, значит персистентность работает по-настоящему, не только в теории. **Частичная живая проверка пройдена 2026-08-19**: `create_task`/`handle_cancel_task` вызваны напрямую с настоящей БД и настоящей моделью — `parse_task("позвонить маме завтра в 18:00")` корректно распознал дату (`2026-08-20 18:00 UTC`, разово), `Task` сохранён, задание реально зарегистрировалось в планировщике (`send_reminder (trigger: date[2026-08-20 18:00:00 UTC], pending)`), отмена корректно убрала задание (`get_job` → `None`). **Отклонение от критерия готовности остаётся**: «напоминание реально приходит в Telegram в заданное время после перезапуска процесса» не проверено целиком технически подтверждённым способом (тестовое напоминание id=4 пропало из очереди планировщика, что косвенно говорит о срабатывании, но прямого подтверждения доставки в Telegram от пользователя не получено). **Баг найден и исправлен 2026-08-20 (живой прод на Beget)**: формулировка «позвонить в банк 13.06» была понята моделью как дата 13 июня **2027 года**, а не время 13:06 сегодня — неоднозначность `ЧЧ.ММ` без явного слова «завтра»/названия месяца модель по умолчанию трактовала как дату. Исправлено: в системный промпт `core/tasks.py` (`SYSTEM_PROMPT_TEMPLATE`) добавлено явное правило — без явного указания на месяц/год трактовать `ЧЧ.ММ` как время, и если получившийся `due_at` больше чем на месяц в будущем без такого явного указания — перепроверять как время. Живая проверка на настоящей модели: `parse_task("позвонить в банк в 13.06")` → `due_at` теперь корректно на сегодня, не на 2027 год. | 🟡 | Нужно подтверждение от пользователя, что реальное напоминание дошло в Telegram в заданное время |

**Найден и исправлен серьёзный пробел со спецификацией 2026-08-19 (параллельная сессия/вкладка)**: постановка напоминания работала только через явную команду `/task`, а `bot/handlers/free_chat.py` (свободный чат, Фаза 4) вообще не знал о существовании задач — сообщение «Напомни в 20:56 позвонить маме», написанное как обычный текст, классифицировалось как заметка, и бот отвечал, что не умеет ставить напоминания. Спецификация (`thoughts/shared/specs/...`, сценарий пользователя, п. 3) прямо описывает естественный язык как основной способ постановки задач — `/task` не должен быть единственным путём, а по факту был. Исправлено: `llm/intent.py` (`detect_intent()` — отдельный дешёвый LLM-вызов, определяет `task`/`chat` до классификации work/personal; выбран отдельный вызов, а не совмещение с `classify_message`, чтобы не тащить лишнее поле в вызовы `classify_message`, где intent уже известен — из `/task` и подтверждения документа). `bot/handlers/tasks_flow.py` отрефакторен — общая логика (`parse_task` + `classify_message` + сохранение + постановка в планировщик) вынесена в `create_task_from_text()`, переиспользуется и командой `/task`, и `free_chat.handle_message`; если намерение «task», но модель не смогла распознать срок — не показываем «не понял срок» человеку, который не пытался ставить задачу, тихо ведём как обычный чат. Тесты: `tests/test_intent.py` (4 теста), `tests/test_tasks_flow.py` (+4 теста на `create_task_from_text`/`describe_schedule` напрямую, все 6 старых тестов прошли без изменений — рефакторинг не сломал публичное поведение `/task`), `tests/test_free_chat.py` (+2 теста — маршрутизация в задачу и откат в чат при нераспознанном сроке). Живая проверка на настоящей БД и модели: «Напомни в 20:56 позвонить маме» обычным сообщением (без `/task`) → `Task` создан с верным временем, `Note` НЕ создана, задание реально встало в `apscheduler_jobs`. Итого 72 теста в проекте, все зелёные (было 62). | 🟡 | Нужно подтверждение доставки тестового напоминания в реальный Telegram (не техническое ограничение — вопрос времени/подтверждения от пользователя) |
| 6. Память и summary диалога | `core/dialog_summary.py` — скользящее текстовое summary, окно/частота пересуммаризации | Сделано: `core/dialog_summary.py` — `record_message()` увеличивает `DialogSummary.message_count_since_update` на каждое сообщение (раздельно по `user_id`+`context`, автосоздаёт строку при первом сообщении); по достижении порога (`MESSAGE_THRESHOLD = 8`, временное значение, открытый вопрос спецификации — уточняется по факту тестирования) пересуммаризирует через LLM последние N заметок (`Note`, уже отфильтрованные по `context` — отдельной таблицы истории сообщений не понадобилось, `Note` уже играет эту роль) и обновляет `summary_text` + сбрасывает счётчик; `get_summary()` — точка доступа для будущих промптов. Подключено в `bot/handlers/free_chat.py` — после сохранения `Note` вызывается `record_message`. **Найден и исправлен архитектурный баг в процессе**: `DialogSummary.updated_at` из Фазы 1 имеет `onupdate=func.now()`, который обновляется на *любое* изменение строки (включая рост счётчика) — окно «сообщения с последнего summary» на этой метке съезжало на каждое сообщение и теряло старые заметки. Решение — не завязываться на `updated_at`, брать последние `MESSAGE_THRESHOLD` заметок по `created_at` напрямую. Тесты: `tests/test_dialog_summary.py` (5 тестов — рост счётчика, пересуммаризация по порогу, сброс без LLM-вызова если заметок нет, раздельность по контекстам). **Отклонение от критерия готовности**: «во втором разговоре бот ссылается на факт со вчера» не проверено — сейчас нет отдельной фазы/хендлера, генерирующего разговорный ответ пользователю (свободный чат в Фазе 4 только классифицирует и подтверждает, не отвечает по существу) — `get_summary()` готов для использования, но подмешивать его в промпт ответа пока некуда; также нужен настоящий `OPENROUTER_API_KEY` для живого прогона резюмирования. *Обновление 2026-08-19*: пробел закрыт — добавлена Фаза 6.5 «Разговорный ответ», критерий готовности «ссылается на факт со вчера» формально перенесён туда, так как по факту требует именно разговорного ответа. **Живая проверка пройдена 2026-08-19**: 8 сообщений подряд (реальная переписка про переезд офиса и уведомление арендодателя) через настоящую БД и модель — на 8-м сообщении `record_message` реально вызвал резюмирование, `summary_text` получил связный текст, верно передающий факты (срок уведомления, дата переезда), раздельность по `context` подтверждена (`summary(personal)` осталась `None`, весь разговор был рабочим). Критерий самой Фазы 6 (механизм резюмирования работает) выполнен; критерий про «ссылается на факт со вчера» — см. Фазу 6.5. | ✅ | — |
| 6.5. Разговорный ответ *(добавлена 2026-08-19)* | `llm/reply.py` (`generate_reply` — содержательный ответ с учётом контекста и summary диалога), `bot/handlers/free_chat.py` — заменить техническое подтверждение на содержательный ответ | Пропущенная фаза — обнаружена и явно подтверждена пользователем при обсуждении Фазы 6 (спецификация с самого начала предполагала «генерацию ответов», см. `thoughts/shared/plans/...`, раздел «Фаза 6.5»). Полный текст задач — там же, не дублируется здесь. Сделано: `llm/reply.py` (`generate_reply(text, context, summary)` — системный промпт учитывает контекст (работа/личное) и, если есть, текущее summary диалога). `bot/handlers/free_chat.py`: `get_summary()` читается до сохранения заметки, `generate_reply()` вызывается вместо технического «Записал как «...»» — ответ бота теперь содержательный, инлайн-кнопка «Это не туда» осталась на том же сообщении (механизм коррекции из Фазы 4 не менялся). Тесты: `tests/test_reply.py` (2 теста — с summary и без), обновлён `tests/test_free_chat.py` (+1 тест на передачу существующего summary в `generate_reply`, остальные адаптированы под новый текст ответа) — итого 48 тестов в проекте, все зелёные. **Живая проверка пройдена 2026-08-19**: после 8 сообщений про переезд офиса (см. Фазу 6) задан контрольный вопрос «напомни, до какого числа надо уведомить арендодателя» — новым сообщением, без повторного объяснения контекста. Ответ бота: «До конца августа нужно отправить уведомление арендодателю — это крайний срок, чтобы соблюсти 30-дневное требование по контракту перед переездом на конец сентября» — верно восстановил факт из summary, не переспрашивая. Критерий готовности выполнен буквально (тот же процесс/сессия вместо «на следующий день» — это по сути тот же сценарий: обращение к summary при новом отдельном сообщении, а не к сырой истории диалога). **Баг найден и исправлен 2026-08-20 (живой прод на Beget)**: на прямой вопрос пользователя «как ты мне напомнишь?» бот ответил, что не умеет присылать напоминания сам — хотя это неправда, Фаза 5 именно это и делает. `generate_reply()` ничего не знает о возможностях `core/tasks.py`/`core/scheduler.py` — это отдельная ветка через `llm/intent.py`, и модель, отвечая в свободном чате, добросовестно не выдумывала, а просто не знала о своей же функции. Исправлено: в `SYSTEM_PROMPT_TEMPLATE` (`llm/reply.py`) добавлен абзац о том, что Арина умеет ставить напоминания по свободной формулировке и сама пишет в нужное время. Живая проверка на настоящей модели: на тот же вопрос теперь отвечает верно — «Я пришлю тебе сообщение прямо в Telegram в нужное время...». | ✅ | — |
| 7. Документы/письма | `llm/documents.py`, `bot/handlers/documents_flow.py` — генерация с нуля + явное подтверждение перед использованием | Сделано: `llm/documents.py` (`generate_document()` — черновик письма/документа с нуля по описанию; модель — `anthropic/claude-sonnet-4.5`, сильнее, чем haiku у классификации/задач/резюмирования — решение этой сессии: для документов важно качество текста, репутационный риск ложится на пользователя после подтверждения). `bot/handlers/documents_flow.py`: `/document <описание>` → генерация → черновик с явной пометкой «это ещё не готовый текст» + кнопки «Подтвердить»/«Переформулировать»; черновик хранится в `context.user_data` (не в БД — эфемерно, не переживает перезапуск процесса, это осознанно: до подтверждения текст не считается ценным для сохранения), подтверждение классифицирует текст через `classify_message` и сохраняет как `Note` (context/личное — так же, как свободные заметки в Фазе 4), переформулировать — просто сбрасывает черновик и просит описание заново. Тесты: `tests/test_documents.py`, `tests/test_documents_flow.py` (9 тестов). Итого 57 тестов в проекте, все зелёные. **Живая проверка пройдена 2026-08-19**: `/document короткое письмо коллеге с переносом созвона на пятницу` на настоящей БД и модели — сгенерирован связный черновик письма, после `handle_confirm_document` он классифицирован и сохранён как `Note`. Критерий готовности выполнен: черновик реально требовал явного подтверждения, а не был сохранён/отправлен автоматически. | ✅ | — |
| 8. Безопасность и 152-ФЗ | Шифрование во всех точках записи, команда полного удаления данных (`/delete_my_data`), проверка отсутствия PII в логах | Сделано, все три пункта техплана закрыты попутно: (1) **Аудит логов после Фаз 3-7** — повторный `grep` по всему проекту (теперь с LLM-кодом, промптами, задачами, документами): по-прежнему нигде не логируется текст сообщений пользователя; единственный `print` — в `tests/calibration/test_calibration.py`, это ручной диагностический вывод на фиксированном синтетическом наборе (не персональные данные реального пользователя), не продакшен-путь. Дополнительно усилено: `openai` SDK на DEBUG может логировать тела запросов (а через них — промпты с текстом сообщений) — явно приглушён до WARNING в `bot/main.py`, как и `httpx`. (2) **Каскадное удаление** — уже было сделано в Фазе 2 (`delete_my_data` в `bot/handlers/onboarding.py` удаляет Task/Note/DialogSummary/User), актуально и сейчас, доп. работы не потребовалось. (3) **Локализация на Beget (РФ)** — не техническая задача этой сессии, проверяется при выборе региона/тарифа хостинга в Фазе 9. Шифрование во всех точках записи (`profile_summary`/`Task.title`/`Note.content`/`DialogSummary.summary_text`) — сделано в Фазе 1, подтверждено на настоящем PostgreSQL. | ✅ | — |
| 9. Закрытая бета | Список приглашённых, деплой на Beget, ручной прогон сценариев, запуск замера retention (1/2/4 недели) | «Список приглашённых» решено не готовить заранее — механизм уже есть (`ALLOWED_USER_IDS`), доступ даётся по мере приглашения, отдельного списка не требуется. Подготовлено для деплоя: `Dockerfile` (устанавливает зависимости, при старте контейнера гоняет `alembic upgrade head` перед запуском бота — идемпотентно, безопасно на каждый рестарт), `docker-compose.prod.yml` (по образцу Aiy 2.0 — Postgres без внешнего порта, только внутри docker-сети), `.env.prod.example` (прод-версия `.env`, отличается хостом БД `postgres` вместо `localhost` и требует `POSTGRES_PASSWORD` для docker-compose), `DEPLOY.md` (пошаговая инструкция). **Найден и исправлен баг при локальной проверке прод-стека**: `pydantic-settings` по умолчанию падает на «лишних» полях в `.env`, а `env_file: .env` в `docker-compose.prod.yml` передаёt в контейнер и `POSTGRES_PASSWORD` (нужен только для docker-compose, не для приложения) — добавлен `extra="ignore"` в `config.py`. Весь прод-стек реально собран и поднят локально (`docker build` + `docker compose -f docker-compose.prod.yml up -d --build`) на тестовом окружении — миграции применились с нуля, планировщик и бот стартовали, порт Postgres наружу не пробрасывается; после проверки стек снят, dev-окружение восстановлено, тестовые данные не смешались с реальными. `scripts/retention_report.py` — отчёт по 1/2/4-недельному retention (активность = создание Task/Note; чистая функция классификации покрыта тестом `tests/test_retention_report.py`, 4 теста). **Обновление 2026-08-20 — реальный деплой выполнен**: пользователь дал доступ к своему VPS (`159.194.214.72`, сервер «BF Group», на нём же уже живут Aiy и content-bot в соседних папках `/opt/aiy`, `/opt/content-bot` — не тронуты). Репозиторий склонирован в `/opt/arina`, `.env` заполнен реальными секретами (свой сгенерированный `POSTGRES_PASSWORD`, тот же `TELEGRAM_BOT_TOKEN`/`OPENROUTER_API_KEY`/`FERNET_KEY`/`ALLOWED_USER_IDS`, что и в локальной разработке), `docker compose -f docker-compose.prod.yml up -d --build` — миграции применились, `arina-bot-1`/`arina-postgres-1` запущены и работают рядом с `aiy_bot`/`aiy_postgres` без конфликтов портов. Онбординг в реальном Telegram с прод-сервера пройден живым пользователем. **Найдена критичная проблема при этом же живом прогоне**: свободные сообщения после онбординга падали с необработанным исключением — причина в `openai.PermissionDeniedError: 403` при обращении к OpenRouter именно с IP этого сервера (см. подробности и статус решения в Факте Фазы 3 и в разделе «Блокер верхнего уровня» выше) — деплой технически работает, но LLM-функциональность в проде не работает, пока не будет готов рабочий релей вне Cloudflare. **Решено 2026-08-20**: релей на Render.com поднят (см. Факт Фазы 3), `OPENROUTER_BASE_URL` на проде переключён на него, контейнер бота пересоздан — LLM-функциональность в проде подтверждена живьём (реальные ответы в Telegram, задачи/напоминания ставятся). Whitelist расширен ещё на один Telegram ID (`5854971023`) по ходу проверки. По пути живым тестированием в реальном Telegram найдены и исправлены ещё два бага (см. Факты Фаз 5 и 6.5): неверное распознавание «13.06» как даты вместо времени, и бот не знал о собственной функции напоминаний при прямом вопросе. | 🟡 | Осталось: подтверждение доставки реального напоминания в срок (Фаза 5) и обновление `.env.prod.example`/`DEPLOY.md` под релей — сейчас не описан |
| 10. Поддержка MAX (мессенджер) *(добавлена 2026-08-20)* | Не входило в исходный план — спецификация явно фиксирует «Telegram-only в MVP». Добавлено по прямому запросу пользователя («давай делать и в max, будет везде арину ставить») — сознательное решение расширить MVP, не пропущенная фаза. | Сделано: `db/models.py` — добавлено поле `User.platform` (`String(16)`, default `"telegram"`), уникальность переведена с одного `telegram_id` на пару `(platform, telegram_id)` — миграция `e91b6261ae03`. **Известное упрощение**: `ALLOWED_USER_IDS` общий на обе платформы (не намеренная фича, а не сделанная отдельно фильтрация) — теоретически MAX ID может случайно совпасть с разрешённым Telegram ID; риск принят как исчезающе малый для закрытой беты из нескольких человек. `max_bot/client.py` (`send_message()` — обёртка над Bot API MAX, `POST /messages?user_id=...`, `platform-api2.max.ru`), `max_bot/webhook.py` (приём вебхуков через `aiohttp`, проверка `MAX_WEBHOOK_SECRET`, роутинг только `message_created`), `max_bot/handlers.py` (переиспользует `llm/classify.py`, `llm/intent.py`, `llm/reply.py`, `core/dialog_summary.py`, `bot/handlers/tasks_flow.create_task_from_text` — та же бизнес-логика, что у Telegram). `core/scheduler.py`: `send_reminder()` теперь смотрит `User.platform` и шлёт через MAX или Telegram. Вебхук-сервер стартует в `Application.post_init` (`bot/main.py`) только если задан `MAX_BOT_TOKEN` — без него всё остальное работает как раньше. **MVP-срез, не полный паритет с Telegram**: есть онбординг, свободный чат с автоклассификацией, задачи/напоминания естественным языком, `/help`, `/delete_my_data`; нет инлайн-кнопок коррекции контекста, `/tasks`, `/document` — сознательно отложено, чтобы не растягивать первый рабочий срез. Технические детали Bot API (эндпоинты, формат вебхука) — из `dev.max.ru/docs-api`, не из training-данных (после дедлайна знаний), проверялись отдельно. Инфраструктура выполнена 2026-08-20: код задеплоен на прод (миграция применилась чисто), в существующий nginx-конфиг `content-bot-site-api` (домен `159-194-214-72.sslip.io`, бесплатный, с готовым Let's Encrypt сертификатом) добавлен новый `location /arina-max/` → `127.0.0.1:8091` — сделана резервная копия файла перед правкой, `nginx -t` проверен до применения, `systemctl reload` без даунтайма. Живая проверка маршрутизации: `curl https://.../arina-max/webhook` → `502` (ожидаемо — сервис за портом ещё не запущен без токена, но сам роутинг подтверждён; content-bot на `location /` не тронут). Тесты: `tests/test_max_client.py`, `tests/test_max_handlers.py` (8 тестов), `tests/test_max_webhook.py` (3 теста, через `pytest-aiohttp`) — итого 86 тестов в проекте на момент коммита, все зелёные. **Отклонение от готовности**: сам вебхук ещё не зарегистрирован в MAX (`POST /subscriptions`) — ждём `MAX_BOT_TOKEN` от пользователя (создаётся через `@MasterBot`), живая проверка отложена по решению пользователя («max потом сделаем»). | 🟡 | Нужен `MAX_BOT_TOKEN` от пользователя, дальше — одна команда регистрации вебхука, план в `DEPLOY.md` |
| 11. Единый часовой пояс *(добавлена 2026-08-20)* | Не входило в исходный план. Предложено ассистентом при вопросе пользователя «что бы ты ещё предложил добавить» — оценено как самый значимый пункт (не косметика, реальный баг). Подтверждено пользователем («продолжай!»). | **Найденная проблема**: всё время внутри бота — UTC, но `core/tasks.py` просил модель переводить формулировку сразу в UTC. Пользователь, говоря «напомни в 15.30», имеет в виду московское время — а получал бы напоминание в 15:30 UTC = 18:30 МСК, на 3 часа позже. Та же проблема — с cron-выражениями для повторяющихся задач (`AsyncIOScheduler` по умолчанию использует часовой пояс процесса, в Docker-контейнере это UTC). И в обратную сторону: `describe_schedule()`/`/tasks` показывали время как есть в БД (UTC) с пометкой «(UTC)» — а должны показывать местное. **Сделано**: `config.py` — `settings.timezone` (`"Europe/Moscow"`, единый на все аккаунты — известное упрощение для MVP, не персональная настройка; аудитория беты русскоязычная). `core/tasks.py`: модель получает текущее время с уже применённым смещением (`+03:00`) и явно просят НЕ переводить самостоятельно в UTC, а использовать то же смещение в ответе — это надёжнее, чем просить модель считать смещение в уме (та же природа ошибки, что и в фиксе «13.06» из Фазы 5). `core/scheduler.py`: `AsyncIOScheduler(timezone=ZoneInfo(settings.timezone))` при создании и `CronTrigger.from_crontab(..., timezone=ZoneInfo(settings.timezone))` — cron-время теперь тоже московское, не UTC процесса. `bot/handlers/tasks_flow.py`: `_format_local()` — `describe_schedule()` и `/tasks` показывают время, сконвертированное в московское, метка «(UTC)» убрана как вводящая в заблуждение (время теперь и так местное). Тесты: +2 в `test_tasks.py` (промпт использует местное время, `+03:00` корректно переводится в 12:00 UTC — абсолютная точка времени, не то же число часов), +2 в `test_scheduler.py` (часовой пояс планировщика и cron-триггера — `Europe/Moscow`), обновлён `test_describe_schedule_one_off` (15:00 UTC → отображается как 18:00 МСК). Живая проверка на настоящей модели: «напомни в 15.30 позвонить в банк» → хранится как `12:30 UTC`, показывается пользователю обратно как `15:30` — точно то время, что просили, без смещения. | ✅ | — |

| 12. Редактирование задач *(добавлена 2026-08-20)* | Не входило в исходный план. Обнаруженный пробел: `/tasks` умел только показывать активные задачи и отменять их («Отменить») — не было способа поправить время/текст уже созданной задачи, только отмена и создание заново. Предложено ассистентом в списке улучшений, продолжение начато без отдельного подтверждения (в рамках установленного в сессии режима «продолжай»). | **Сделано**: `bot/handlers/tasks_flow.py` — новая кнопка «Изменить» рядом с «Отменить» в `list_tasks()` (`callback_data=f"edit_task:{task.id}"`); `handle_edit_task_button()` проверяет, что задача принадлежит нажавшему (сверка `telegram_id`), кладёт `task_id` в `context.user_data[EDIT_PENDING_KEY]` и просит прислать новую формулировку; `apply_task_edit(user_id, task_id, text)` — переразбирает присланный текст через тот же `parse_task`/`classify_message`, что и создание новой задачи, обновляет `title`/`context`/`due_at`/`recurrence_rule` на месте и переставляет напоминание в планировщике (`schedule_task_reminder` с `replace_existing=True`); возвращает `None` без изменений, если модель не распознала срок, или если задача не найдена/чужая. `bot/handlers/free_chat.py`: `handle_message()` в самом начале проверяет `context.user_data.pop(EDIT_PENDING_KEY, None)` — если есть ожидающее редактирование, весь текст идёт в `apply_task_edit()` вместо обычного определения намерения (`detect_intent`), после чего либо подтверждает новое время, либо сообщает, что не понял и задача осталась прежней. `bot/main.py`: зарегистрирован `CallbackQueryHandler(handle_edit_task_button, pattern=r"^edit_task:")`. Тесты: +5 в `tests/test_tasks_flow.py` (обновление существующей задачи, отказ без распознанного срока, отказ для чужой задачи, подсказка по кнопке, отказ по кнопке для чужой задачи), +2 в `tests/test_free_chat.py` (применение ожидающего редактирования, сообщение о нераспознанном времени) — по ходу пришлось завести `make_context()` (реальный словарь `user_data` вместо `context=None`, использовавшегося в тестах этого файла ранее — `handle_message` теперь читает `context.user_data`). Итого 96 passed + 1 skipped. Живая проверка на реальном проде не проводилась — ждёт подтверждения пользователем через настоящий Telegram. | ✅ | — |

| 13. Голосовые сообщения *(добавлена 2026-08-20)* | Не входило в исходный план. Прямая просьба пользователя: «сделай голосовое принятие сообщение от клиента». | **Сделано**: изначально распознавание речи планировалось через отдельный ключ OpenAI Whisper API (см. историю ниже) — пользователь скорректировал: «OpenRouter делай через него». Проверено через `WebSearch`/`WebFetch` (`openrouter.ai/docs/guides/overview/multimodal/stt`): у OpenRouter есть выделенный `POST /api/v1/audio/transcriptions`, совместимый с `openai` SDK (`client.audio.transcriptions.create()` работает, если просто указать `base_url=https://openrouter.ai/api/v1`), модель — `openai/whisper-1`. Это отменяет саму необходимость в отдельном ключе/сервисе — переиспользуется тот же `OPENROUTER_BASE_URL`/`OPENROUTER_API_KEY`, что и для текста, и тот же прод-релей на Render (`relay.py` — проксирует TARGET+`self.path` для любого пути, не только `/chat/completions`, значит `/audio/transcriptions` тоже проходит без доработок). `config.py`: `openai_base_url`/`openai_api_key` добавленные для первого варианта — удалены за ненадобностью, `.env.example` тоже откачен. `llm/transcribe.py`: `transcribe_voice(audio_bytes)` — вызывает `openai/whisper-1` через `AsyncOpenAI(base_url=settings.openrouter_base_url, api_key=settings.openrouter_api_key)` (тот же клиент, что `llm/client.py`), с тем же паттерном ретраев/`LLMUnavailableError`, язык не указывается явно (Whisper определяет сам). `bot/handlers/free_chat.py`: тело `handle_message` вынесено в `_process_text(telegram_id, text, update, context)` — общий код для текста и голоса (включая ветку ожидающего редактирования задачи из Фазы 12: голосом можно и создавать, и редактировать задачи). `handle_voice_message()` — новый хендлер: проверка whitelist → `context.bot.get_file()` → `download_as_bytearray()` → `transcribe_voice()` → тот же `_process_text()`, что и для текстовых сообщений. `bot/main.py`: зарегистрирован `MessageHandler(filters.VOICE, handle_voice_message)`. `/help` (`bot/handlers/onboarding.py`) дополнен строкой про голосовые сообщения и про кнопку «Изменить» в /tasks (не была упомянута при добавлении Фазы 12). Тесты: `tests/test_transcribe.py` (3 теста — успех, ретрай, исчерпание попыток с русской ошибкой, по образцу `tests/test_llm_client.py`), +3 в `tests/test_free_chat.py` (распознавание и обработка как текст, игнор не-whitelisted без траты на распознавание, сообщение об ошибке сети). Итого 118 passed + 1 skipped. **Известное упрощение**: голосовые не сохраняются как файлы — только распознанный текст, аудио не хранится нигде после обработки. **Живая проверка пройдена 2026-08-20**: синтетический WAV (тон 440 Гц, без реальной речи — генерирован локально только чтобы проверить сам путь запроса, не качество распознавания) отправлен через `transcribe_voice()` на настоящий прод-релей и настоящий `OPENROUTER_API_KEY` — запрос принят без ошибок авторизации/сети, вернулся валидный JSON-ответ с текстом. Так как используется уже проверенный на доступность из Beget релей, отдельной проверки геоблокировки не требуется — вопрос снят самим выбором OpenRouter. **Баг найден и исправлен 2026-08-20 (живой прод)**: при реальной проверке пользователем голосовое сообщение вообще не доходило ни до одного хендлера — в логах ни следа попытки распознавания, ни ошибки, полная тишина. Причина: `filters.VOICE` матчит только нативные голосовые заметки (`.voice`), а не аудиофайлы (`.audio`) — вероятно, сообщение ушло не тем способом. Исправлено: фильтр расширен до `filters.VOICE | filters.AUDIO`; `handle_voice_message` берёт `update.message.voice or update.message.audio`; `run_polling(allowed_updates=Update.ALL_TYPES)` — явный запрос всех типов обновлений, чтобы исключить эту причину полностью; добавлен `handle_unsupported_message` — catch-all хендлер на любой нераспознанный формат сообщения (видео-кружок, стикер, фото), отвечающий понятной фразой вместо тишины бота, чтобы будущие похожие сбои сразу были видны пользователю, а не выглядели как «ничего не произошло». Тесты: +4 в `tests/test_free_chat.py` (аудиофайл вместо voice, ответ-подсказка на неподдержанный формат, игнор не-whitelisted для обоих новых путей). Итого 121 passed + 1 skipped. | ✅ | — |

| 14. Учёт бюджета *(добавлена 2026-08-20)* | Не входило в исходный план. Прямая просьба пользователя, с явным указанием сделать MVP-срез по аналогии с Фазой 10 (MAX) — без банковской интеграции, баланс сообщает сам пользователь. | **Сделано**: `db/models.py` — `TransactionType` (`expense`/`income`), `Transaction` (`user_id`, `amount` — `Numeric(12,2)`, `transaction_type`, `description` — зашифровано `EncryptedString`, как `Note.content`, `created_at`); `User` получил `balance` и `low_balance_threshold` (оба `Numeric(12,2)`, не зашифрованы — числа, а не свободный текст, та же логика, что у `Task.due_at`/`TaskStatus` в существующих моделях). Alembic-миграция `5cc8b2c5aee2` — добавлена вручную (не автогенерацией — окружение без прямого доступа к чистой БД под автогенерацию в этот момент), проверена на настоящем локальном PostgreSQL: `upgrade`/`downgrade`/повторный `upgrade` — все прошли, `alembic check` не находит расхождений кроме уже известного `apscheduler_jobs` (runtime-таблица APScheduler, не часть моделей — та же оговорка, что в `e91b6261ae03`). `core/finance.py` — `parse_finance_message(text)` (по образцу `core/tasks.py::parse_task`), различает три случая одним промптом к LLM: `"transaction"` (трата/приход — извлекает `amount`/`transaction_type`/`category`), `"balance"` (сообщённый остаток), `"threshold"` (порог для предупреждения); возвращает `None`, если сумму/тип не удалось разобрать. `llm/intent.py` — `Intent` расширен третьим значением `finance`, промпт дополнен признаками (сообщение о конкретной сумме/остатке — не абстрактное обсуждение бюджета, которое остаётся `chat`). `bot/handlers/finance_flow.py` — `record_finance_message(user_id, text)`: разбирает через `parse_finance_message`, для `balance` обновляет `User.balance` и сразу же (в том же ответе) проверяет `low_balance_threshold` — если баланс ниже, дописывает предупреждение «⚠️»; для `threshold` обновляет `User.low_balance_threshold`; для `transaction` сохраняет `Transaction`. `bot/handlers/free_chat.py::_process_text` — добавлена ветка `elif intent == Intent.finance`, с тем же паттерном отказоустойчивости, что у `Intent.task` (если `record_finance_message` вернул `None` — не показываем «не понял», ведём как обычный чат). Напоминания об оплате кредита/коммуналки — новый код не понадобился, это обычные повторяющиеся задачи из Фазы 5; `/help` (`bot/handlers/onboarding.py`) дополнен абзацем с примерами всех трёх сценариев и явной отсылкой к повторяющимся задачам для регулярных платежей. **Сознательно не сделано (известное упрощение)**: нет автоматического пересчёта баланса из суммы транзакций — баланс всегда равен тому, что пользователь последний раз назвал сам, а не агрегату по `Transaction`; нет банковской интеграции; нет аналитики/графиков по категориям — `Transaction` пока используется только как лог. Тесты: `tests/test_finance.py` (6 тестов на `parse_finance_message` — расход, приход, баланс, порог, `None` при отсутствии суммы, `ValueError` при непарсящемся ответе), `tests/test_finance_flow.py` (7 тестов на `record_finance_message` — сохранение расхода/прихода, обновление баланса, предупреждение о низком балансе и его отсутствие выше порога, установка порога, `None` при нераспознанном сообщении), +1 в `tests/test_intent.py` (распознавание `finance`), +2 в `tests/test_free_chat.py` (маршрутизация в `record_finance_message`, откат к чату при нераспознанном finance-сообщении). Итого 118 passed + 1 skipped. **Живая проверка на настоящей модели и настоящем PostgreSQL пройдена 2026-08-20**: 4 сообщения на `detect_intent` — все верно распознаны как `finance`; `record_finance_message` на реальном ключе: «потратила 500 в Пятёрочке» → «Записал расход: 500 ₽ (Пятёрочка).», сохранено в БД с верной расшифровкой категории; «предупреждай, если останется меньше 2000» → порог сохранён; «у меня на счёте осталось 1500» → баланс обновлён И в том же ответе добавлено предупреждение о балансе ниже порога — критерий готовности «предупреждение сразу после сообщённого баланса» подтверждён на реальном стеке. Синтетический пользователь и его записи удалены после проверки. | ✅ | — |

| 15. Меню команд Telegram *(добавлена 2026-08-20)* | Не входило в исходный план. Прямая просьба пользователя, первая из списка «несколько дополнений подряд» — штатный механизм Telegram (`setMyCommands`) вместо кастомных inline-кнопок. | **Сделано**: `bot/main.py` — `BOT_COMMANDS` (список `BotCommand` с русскими описаниями: `/start`, `/help`, `/task`, `/tasks`, `/document`, `/delete_my_data`), вызывается через `application.bot.set_my_commands(BOT_COMMANDS)` в начале `_on_startup()` (до запуска планировщика/MAX-вебхука). Тесты: `tests/test_main.py` (новый файл, 3 теста — `set_my_commands` вызван с верным списком и у каждой команды есть непустое описание, планировщик всё ещё стартует, `build_application()` регистрирует хендлеры). Итого 124 passed + 1 skipped. **Живая проверка пройдена 2026-08-20**: `set_my_commands`/`get_my_commands` вызваны напрямую на настоящем `TELEGRAM_BOT_TOKEN` — меню подтверждено с сервера Telegram, все 6 команд с точными русскими описаниями на месте. | ✅ | — |

| 16. Документы в реальных файлах (Word/Excel/PDF) *(добавлена 2026-08-20)* | Не входило в исходный план. Прямая просьба пользователя, вторая из списка «несколько дополнений подряд». | **Сделано**: `llm/documents.py::generate_document()` теперь возвращает не голый текст, а структуру `ParsedDocument` (`format`/`title`/`content`) — LLM сама определяет формат по описанию (docx для писем/договоров, xlsx для таблиц/смет, pdf для документов, которые обычно не редактируют), тем же приёмом структурированного JSON-вывода, что `core/tasks.py`/`core/finance.py`; для xlsx content — простой формат "строка на строку, ячейки через `\|`" (сознательно не вложенный JSON-массив — надёжнее для LLM). `core/document_files.py` (новый модуль) — `build_docx`/`build_xlsx`/`build_pdf`/`build_document_file()`: `python-docx`, `openpyxl`, `reportlab`. Для PDF пришлось решить проблему кириллицы — встроенные шрифты reportlab (Helvetica и т.п.) не содержат кириллических глифов; решено подключить DejaVu Sans (обычный + жирный) как TTF-шрифт через `pdfmetrics.registerFont`. Шрифт взят из дистрибутива matplotlib (тот же файл, что использует множество Python-проектов для кириллицы в PDF) и закоммичен в `assets/fonts/` — matplotlib НЕ добавлен в зависимости проекта, использован только один раз локально, чтобы скопировать файл шрифта; лицензия DejaVu Fonts License (свободная, разрешает встраивание) — см. `assets/fonts/README.md`. `bot/handlers/documents_flow.py`: логика `/document` вынесена в общую `start_document_draft(update, context, description)` (по образцу `create_task_from_text`/`apply_task_edit` — переиспользуется и командой, и свободным чатом/голосом); `handle_confirm_document` теперь после подтверждения строит настоящий файл через `build_document_file()` и отправляет `reply_document()` — вместо текста в чате пользователь получает реальный `.docx`/`.xlsx`/`.pdf`. Подтверждение перед генерацией осталось обязательным (черновик текста показывается для проверки до того, как строится файл — CLAUDE.md требует, чтобы документ никогда не считался готовым к использованию автоматически). `llm/intent.py` — `Intent` расширен четвёртым значением `document` (после `task`/`finance`/`chat`), с промптом различающим явную просьбу подготовить документ от простого рассуждения о документах (остаётся `chat`). `bot/handlers/free_chat.py::_process_text` — ветка `elif intent == Intent.document` вызывает `start_document_draft` — благодаря этому голосовые сообщения (Фаза 13) автоматически заработали и для документов, отдельного кода на голос не понадобилось, как и предполагалось в постановке задачи. Зависимости добавлены в `requirements.txt`: `python-docx`, `openpyxl`, `reportlab`, `pypdf` (последний — только для тестов, проверка читаемости сгенерированного PDF). Тесты: `tests/test_document_files.py` (5 тестов — содержимое docx/xlsx, кириллица в PDF читается через `pypdf`, маршрутизация по формату, ошибка на неизвестном формате), `tests/test_documents.py` переписан под `ParsedDocument` (3 теста — docx, xlsx, `ValueError` на непарсящийся ответ), `tests/test_documents_flow.py` обновлён под новый пайплайн (правильный вызов `build_document_file`, отправка файла вместо текста), +1 в `tests/test_intent.py`, +1 в `tests/test_free_chat.py` (маршрутизация `document`-намерения). Итого 134 passed + 1 skipped. **Живая проверка на настоящей модели пройдена 2026-08-20**: сгенерированы настоящие `.docx` (письмо о переносе встречи) и `.xlsx` (смета расходов на март) через реальный `OPENROUTER_API_KEY`/прод-релей, файлы открыты обратно через `python-docx`/`openpyxl` — содержимое корректное, кириллица не искажена; PDF-путь проверен через `pypdf` в тестах (кириллица в шрифте DejaVu читается верно), отдельного живого PDF-прогона с реальной моделью не делала (сама генерация текста для PDF идёт по тому же пути, что и для docx — риск отдельный только в шрифте, а он уже проверен тестом). `detect_intent` на трёх реальных фразах — все три верно классифицированы, включая отрицательный случай («что думаешь про документооборот» → `chat`, не `document`). | ✅ | — |

| 17. Пересылка сообщений между пользователями Арины *(добавлена 2026-08-20)* | Не входило в исходный план. Прямая просьба пользователя, третья из списка «несколько дополнений подряд» — только между пользователями из `ALLOWED_USER_IDS`, по @username, с обязательным подтверждением и явной пометкой получателю. | **Сделано**: `db/models.py` — `User.username` (обычная строка, НЕ зашифровано: нужен точный поиск по значению, а Fernet-шифрование недетерминированное — WHERE по зашифрованному полю не сработает, та же причина, что у `telegram_id`/`platform`; обновляется на каждый `/start`, может немного отставать при смене username без повторного `/start` — известное упрощение). Alembic-миграция `74d805f803d0`, проверена на настоящем локальном PostgreSQL (`upgrade`/`downgrade`/повторный `upgrade`, `alembic check` — только уже известный `apscheduler_jobs`). `bot/handlers/onboarding.py` — `username` сохраняется в `receive_profile()` (создание) и обновляется в `start()` при возврате уже онбордившегося пользователя. `core/relay.py` — `parse_relay_message(text)` (структурированный JSON, как `core/finance.py`) извлекает `username` (без `@`, `null` если не указан явно) и текст для пересылки. `bot/handlers/relay_flow.py` — `start_relay_draft()`: онбординг-гейт → парсинг → если `username` не распознан, просит указать `@username` явно → ищет получателя по `User.username` → **тройная проверка получателя** (найден в БД, его `telegram_id` всё ещё в `settings.allowed_user_ids_list`, `onboarding_completed=True`) — это и есть ограничение «только между пользователями из ALLOWED_USER_IDS» из постановки задачи, реализованное как самая безопасная проверка на момент отправки, а не на момент регистрации; отдельно отклоняет попытку переслать самому себе. Найден получатель → черновик в `context.user_data[PENDING_KEY]` (`PendingRelay` — `recipient_user_id`/`recipient_username`/`message`) → показывает точный текст с кнопками «Отправить»/«Переформулировать» (тот же confirm-паттерн, что `documents_flow.py`, свои callback-имена `confirm_relay`/`reformulate_relay` во избежание коллизий). `handle_confirm_relay()` — на подтверждение шлёт `context.bot.send_message(chat_id=recipient.telegram_id, text=...)` с обязательным префиксом `📨 Сообщение от @username через Арину:` (пометка получателю, что сообщение переслано через Арину, а не написано ботом от себя — требование из постановки задачи), без try/except вокруг `send_message` — тот же паттерн, что уже есть в `core/scheduler.py::send_reminder` для доставки напоминаний, сбой уйдёт в общий `_on_error`. `llm/intent.py` — `Intent` расширен пятым значением `relay`, с явным требованием в промпте: без `@username` в тексте это не relay, даже если упомянуто имя без username (иначе легко спутать с `document` — «напиши Ивану письмо» и «передай Ивану, что...» звучат похоже, но intent должен различать голое имя от `@username`). `bot/handlers/free_chat.py::_process_text` — ветка `elif intent == Intent.relay` вызывает `start_relay_draft` (сама обрабатывает все исходы и ошибки сети), значит голос тоже работает для пересылки без дополнительного кода, как и для `document`/`task`/`finance`. `bot/main.py` — зарегистрированы `handle_confirm_relay`/`handle_reformulate_relay`. `/help` дополнен абзацем. Тесты: `tests/test_relay.py` (3 теста на `parse_relay_message`), `tests/test_relay_flow.py` (11 тестов — онбординг-гейт, ошибка сети, отсутствие `@username`, неизвестный получатель, получатель вне whitelist, попытка переслать себе, успешный черновик с сохранением `PendingRelay`, подтверждение без черновика, успешная отправка с проверкой текста/префикса/`chat_id`, переформулирование), +2 в `tests/test_onboarding.py` (обновление `username` при повторном `/start`, сохранение при создании), +1 в `tests/test_intent.py`, +1 в `tests/test_free_chat.py`. Итого 150 passed + 1 skipped. **Живая проверка на настоящей модели и настоящем PostgreSQL пройдена 2026-08-20**: `detect_intent`/`parse_relay_message` на трёх реальных фразах — верно распознали `relay` с `@username` в обоих случаях и корректно отличили от `chat` фразу без `@` («передай Ивану, что я задерживаюсь» — без явного username, как и должно быть по промпту). Полный цикл `start_relay_draft` → `handle_confirm_relay` прогнан на синтетических пользователях в реальном PostgreSQL (реальный вызов `parse_relay_message` на настоящей модели, не мок) — черновик собран верно, текст на отправку сформирован с верным префиксом и `chat_id`; синтетические записи удалены после проверки. **Не проверено живьём** (нужны два реальных аккаунта из `ALLOWED_USER_IDS`, пересылающих друг другу через настоящий Telegram): сам факт получения сообщения на телефоне получателя — сама отправка через Bot API до этого шага домокана намеренно, так как у ассистента нет второго реального аккаунта для приёма. | 🟡 | Нужна подтверждающая живая проверка от пользователя: два реальных аккаунта из `ALLOWED_USER_IDS`, один просит переслать сообщение другому по `@username`, получатель подтверждает получение через настоящий Telegram |

| 18. Email-напоминания для контактов без Арины *(добавлена 2026-08-20)* | Не входило в исходный план. Прямая просьба пользователя, четвёртая из списка «несколько дополнений подряд» — SMS сознательно отложены на потом (см. постановку задачи). | **Сделано**: выбор способа отправки — транзакционный сервис (не SMTP, ради доставляемости) — согласован с пользователем через `AskUserQuestion`. Провайдер — Resend: простой REST API (`POST https://api.resend.com/emails`, Bearer-токен, JSON `{from, to, subject, text}`), выбран без установки SDK — обычный `httpx.AsyncClient`, тот же паттерн, что уже есть в `max_bot/client.py`. `config.py` — `resend_api_key`/`email_from_address`, пустые по умолчанию (тот же паттерн, что `MAX_BOT_TOKEN`: без ключа функция недоступна, но не ломает остальной бот). `core/email_client.py` — `send_email(to, subject, body)`, `EmailUnavailableError` при отсутствии конфигурации, сетевой ошибке или ошибке статуса от API. `core/email_relay.py` — `parse_email_message(text)` (структурированный JSON, как `core/relay.py`) извлекает email-адрес (`null`, если не указан явно, дополнительно перепроверяется регэкспом на стороне кода — на случай, если модель всё же вернёт что-то похожее на адрес, но не прошедшее валидацию), тему и текст письма. `db/models.py` — `EmailLog` (НЕ адресная книга — лог уже отправленных писем: `recipient_email`/`subject`/`body` зашифрованы `EncryptedString`, как остальные персональные данные, per CLAUDE.md; получатель не переиспользуется для поиска — email указывается заново в каждом запросе, той же логики, что и `Transaction`/`Note` — только запись, не справочник). Alembic-миграция `e19935ff59f9`, проверена на настоящем локальном PostgreSQL (`upgrade`/`downgrade`/повторный `upgrade`, `alembic check` — только уже известный `apscheduler_jobs`). `bot/handlers/email_flow.py` — `start_email_draft()`/`handle_confirm_email()`/`handle_reformulate_email()`, тот же confirm-паттерн, что `documents_flow.py`/`relay_flow.py` (обязательное подтверждение черновика перед отправкой — письмо не считается готовым к отправке автоматически, CLAUDE.md), лог пишется только после успешной отправки (если `send_email` упал — ничего не сохраняется). `llm/intent.py` — `Intent` расширен шестым значением `email`, с явным разграничением от `relay` в промпте (email — настоящий адрес с доменом через точку, relay — `@username` в Telegram без домена). `bot/handlers/free_chat.py::_process_text` — ветка `elif intent == Intent.email`, голос работает автоматически, как и для остальных намерений. `bot/main.py` — зарегистрированы `handle_confirm_email`/`handle_reformulate_email`. `/help` дополнен абзацем. Тесты: `tests/test_email_client.py` (4 теста — успешная отправка, ошибка статуса API, сетевая ошибка, отсутствие конфигурации), `tests/test_email_relay.py` (4 теста — извлечение адреса/темы/текста, `None` без адреса, отклонение некорректного адреса, `ValueError` на непарсящийся ответ), `tests/test_email_flow.py` (8 тестов — онбординг-гейт, ошибка сети при разборе, отсутствие адреса, успешный черновик, подтверждение без черновика, успешная отправка+лог, ошибка отправки без записи в лог, переформулирование), +1 в `tests/test_intent.py`, +1 в `tests/test_free_chat.py`. Итого 168 passed + 1 skipped. **Живая проверка на настоящей модели и настоящем PostgreSQL пройдена 2026-08-20**: `detect_intent`/`parse_email_message` на реальных фразах — верно распознали `email` и корректно отличили от `relay` (`@username`) и `task`; сгенерированный текст письма — связный и уместный по тону. Полный цикл `start_email_draft` → `handle_confirm_email` прогнан на синтетическом пользователе в реальном PostgreSQL (реальный вызов `parse_email_message` на настоящей модели) — черновик, лог и шифрование/расшифровка `EmailLog` отработали верно; синтетические записи удалены после проверки. **Не проверено живьём**: сама доставка через Resend API — ключа `RESEND_API_KEY` пока нет (ждёт от пользователя), `send_email` в живой проверке был замокан намеренно. | 🟡 | Нужен `RESEND_API_KEY` и `EMAIL_FROM_ADDRESS` (домен, верифицированный в Resend, либо тестовый адрес `onboarding@resend.dev` на первое время) от пользователя — без них функция задеплоена, но неактивна (`EmailUnavailableError` при попытке использовать); после получения ключа нужна живая отправка на реальный адрес для итогового подтверждения |

| 19. Полная память без сжатия *(добавлена 2026-08-20)* | Не входило в исходный план. Прямая просьба пользователя, пятая из списка «несколько дополнений подряд» — осознанный отказ от LLM-пересуммаризации (Фаза 6) в пользу хранения без потерь; открытый вопрос — что реально попадает в промпт, чтобы не упереться в стоимость/лимит токенов — требовалось решить и задокументировать. | **Решение**: полная история уже и так хранилась без потерь в `Note` (ничего не удаляется, кроме `/delete_my_data`) — сжатию подвергалось не хранение, а то, что *подмешивалось в промпт*: `core/dialog_summary.py` каждые `MESSAGE_THRESHOLD=8` сообщений просило LLM пересуммаризировать последние заметки в 3-5 предложений, теряя детали. Убрано полностью — вместо скользящего LLM-summary теперь **окно последних сообщений без сжатия**: `get_recent_context(user_id, context)` берёт последние `MAX_RECENT_MESSAGES=30` заметок из `Note` (тот же источник, что и раньше) и передаёт их в промпт как есть, дословно, без LLM-обработки; дополнительно обрезается по `MAX_CONTEXT_CHARS=8000` символов (защита от одного аномально длинного сообщения, не от количества) — это и есть ответ на открытый вопрос: не вся история идёт в каждый промпт (это было бы неограниченно дорого/долго со временем), а последние ~30 сообщений дословно; за пределами этого окна модель ничего не "помнит" в рамках одного вызова — осознанный, а не забытый компромисс. **Сделано**: `db/models.py` — класс `DialogSummary` удалён; Alembic-миграция `e76468fc7fa9` дропает таблицу `dialog_summaries` (проверено `upgrade`/`downgrade`/повторный `upgrade`, `alembic check` — только уже известный `apscheduler_jobs`; существующий кеш summary отброшен осознанно — это был кеш, не источник истины, сама история осталась в `Note` нетронутой). `core/dialog_summary.py` переписан: `record_message()`/`_resummarize()`/`MESSAGE_THRESHOLD` удалены (не нужны — сохранение `Note` уже было отдельным шагом от подсчёта/пересуммаризации), `get_summary()` заменена на `get_recent_context()`. `llm/reply.py::generate_reply()` — параметр переименован `summary` → `recent_messages`, текст в промпте изменён с "краткое summary предыдущих разговоров" на "последние сообщения пользователя" — модели явно объяснено, что это сырые сообщения, а не сгенерированное резюме. `bot/handlers/free_chat.py`/`max_bot/handlers.py` — вызовы `get_summary`/`record_message` заменены на `get_recent_context`, отдельный вызов "записать сообщение для счётчика" убран за ненадобностью. **Попутно найден и исправлен пробел**: `bot/handlers/onboarding.py::delete_my_data` и `max_bot/handlers.py::_delete_my_data` никогда не удаляли `Transaction`/`EmailLog` (появившиеся в Фазах 14/18, уже после того как `delete_my_data` в последний раз редактировался) — учитывая гарантию 152-ФЗ "полностью удалю все ваши данные", это было реальным пробелом, а не придиркой; исправлено заодно, раз функция уже редактировалась. **Также найдена и исправлена по ходу нестабильность сортировки**: `get_recent_context()` сортировал только по `Note.created_at`, а `CURRENT_TIMESTAMP` в SQLite (и потенциально при частых записях в Postgres) имеет точность до секунды — два сообщения в одну секунду давали недетерминированный порядок; добавлен `Note.id.desc()` вторым ключом сортировки. Тесты: `tests/test_dialog_summary.py` переписан полностью под `get_recent_context` (5 тестов — пусто без заметок, хронологический порядок, раздельность по контексту, обрезка по количеству, обрезка по символам), `tests/test_free_chat.py`/`tests/test_max_handlers.py` обновлены под новую сигнатуру, `tests/test_onboarding.py`/`tests/test_max_handlers.py` дополнены проверкой удаления `Transaction`/`EmailLog` в `delete_my_data`. Итого 168 passed + 1 skipped (без изменения количества — тесты пересозданы 1:1, не добавлены сверх). **Живая проверка на настоящей модели и настоящем PostgreSQL пройдена 2026-08-20**: воспроизведён тот же сценарий, что при живой проверке Фазы 6.5 (переезд офиса, уведомление арендодателя за 30 дней) — три сообщения сохранены как обычные `Note`, `get_recent_context()` вернул их дословно без каких-либо LLM-вызовов на пересуммаризацию, контрольный вопрос «напомни, до какого числа надо уведомить арендодателя» получил верный ответ («До конца августа... 30 дней... переезд в конце сентября») — то же качество памяти, что раньше давало сжатое summary, но без единого шага потери деталей. CLAUDE.md обновлён (структура проекта, описание памяти в разделе «О проекте», формулировка про шифруемые поля) — старые упоминания "скользящего summary" исправлены на актуальное поведение. | ✅ | — |

| 20. Адаптация стиля общения под пользователя *(добавлена 2026-08-20)* | Не входило в исходный план. Прямая просьба пользователя, последняя из списка «несколько дополнений подряд» — явно указано не оформлять «незаменимость» отдельной задачей, это результат совокупности функций плюс этой персонализации, а не отдельная техническая строка кода. | **Сделано**: `llm/reply.py::generate_reply()` дополнен новым параметром `profile_summary: str | None = None` (после уже существующего `recent_messages`) — используются два уже собранных источника, ничего нового не хранится: `User.profile_summary` (чем занимается пользователь, собрано при онбординге, Фаза 2) и то же окно последних сообщений из Фазы 19 (`get_recent_context`), которое теперь служит двум целям — памяти о разговоре И ориентиром по манере письма пользователя (эту вторую цель явно называет промпт). `SYSTEM_PROMPT_TEMPLATE` дополнен абзацем: подстраиваться под то, как пишет конкретный пользователь — формально/неформально, коротко/развёрнуто, с эмодзи или без, — явно требуя различия между разными пользователями вместо одного стиля на всех; отдельно оговорено не копировать фразы дословно, ориентироваться на тон/длину/манеру. Убрано слово «коротко» из общей инструкции по умолчанию (раньше длина ответа была жёстко задана всем одинаково — теперь длина сама подстраивается под наблюдаемый стиль). `bot/handlers/free_chat.py`/`max_bot/handlers.py` — `profile_summary` читается из уже загруженного `User` (тот же запрос, что и на проверку `onboarding_completed`, без лишнего похода в БД) и передаётся в `generate_reply()`. Тесты: +2 в `tests/test_reply.py` (профиль попадает в промпт, блок профиля отсутствует, если профиля нет), +1 в `tests/test_free_chat.py` (`profile_summary` реально доходит до вызова `generate_reply` с непустым значением), обновлены 2 существующих теста под новую 4-параметрическую сигнатуру. Итого 171 passed + 1 skipped. **Живая проверка на настоящей модели пройдена 2026-08-20**: один и тот же вопрос («что там по задачам на сегодня») задан от лица двух разных синтетических профилей с разной историей сообщений — «юрист, формальный деловой стиль» (история сообщений формальная, развёрнутая) и «фрилансер-дизайнер, неформально, коротко, с эмодзи» (история — сокращения, разговорный тон). Ответы получились заметно разными: формальному пользователю — обращение на «вы», структурированный нумерованный список, без эмодзи; неформальному — разговорный тон, короче, с эмодзи (📋 👀) — критерий готовности («не звучит одинаково у всех») подтверждён на реальном стеке. Прямая заявка на «незаменимость» сознательно не сделана отдельной задачей, как и просил пользователь — эффект достигается совокупностью памяти/задач/документов/финансов и этой персонализации. | ✅ | — |

| 21. Женский род в самоописании Арины *(добавлена 2026-08-20)* | Не входило в исходный план. Прямая просьба пользователя: в русском языке род важен в прошедшем времени и кратких прилагательных («поняла»/«понял», «готова»/«готов»), нужно явно закрепить женский род и проверить остальные промпты. | **Найдено при аудите**: помимо `llm/reply.py` (единственное место, где модель говорит от лица Арины в свободной форме — исправлено явной инструкцией в `SYSTEM_PROMPT_TEMPLATE`), обнаружился более широкий и более реальный источник проблемы — **захардкоженные строки-ответы бота** использовали мужской род на протяжении нескольких фаз: «Записал:», «Обновил:», «Не понял срок/время», «Отменил.» в `bot/handlers/tasks_flow.py`, `bot/handlers/free_chat.py`, `bot/handlers/finance_flow.py`, `max_bot/handlers.py` — все восемь вхождений исправлены на «Записала»/«Обновила»/«Не поняла»/«Отменила». **Проверены и оставлены как есть (осознанно)**: `llm/documents.py` и `core/email_relay.py` генерируют текст письма/документа не от лица Арины, а от лица самого пользователя (письмо, которое пользователь отправит от своего имени) — форсировать женский род там было бы неверно, если сам пользователь другого рода; `bot/handlers/relay_flow.py`/`bot/handlers/email_flow.py` уже были написаны корректно в женском роде («Не поняла», «Не нашла») с самого начала (Фазы 17/18) — совпадение, но не ошибка. `llm/classify.py`/`llm/intent.py`/`core/tasks.py`/`core/finance.py`/`core/relay.py` — чисто структурированный JSON-вывод без свободного текста от лица Арины, гендерных форм нет в принципе. Тесты: обновлена 1 assert-проверка точного текста в `tests/test_tasks_flow.py` (`"Отменила."` вместо `"Отменил."`) — остальные упоминания старых мужских форм в тестах оказались произвольными мок-строками (`AsyncMock(return_value=...)`), не проверяющими реальный текст ответа, менять не требовалось. Итого 171 passed + 1 skipped (без изменения количества). **Живая проверка на настоящей модели пройдена 2026-08-20**: три вопроса с прошедшим временем/кратким прилагательным от первого лица Арины («ты уже прочитала моё сообщение?», «готова помочь с отчётом?», «что подумала о моей идее?») — во всех трёх ответах модель употребила корректные женские формы («увидела», «готова», «не видела») без единого сбоя. | ✅ | — |

Статусы: ⬜ Не начато · 🟡 В работе · ✅ Готово · 🔴 Заблокировано

## Следующее конкретное действие

**Состояние на 2026-08-20 (конец дня):** Фазы 0-9 закрыты на `✅`/`🟡` (все технически
готовы, детали в таблице выше — только Фаза 5 ждёт подтверждения доставки реального
напоминания в срок). Прод реально работает на Beget VPS (`159.194.214.72`,
`@arina_bf_bot`, рядом с Aiy/content-bot, не пересекаясь): онбординг, свободный чат,
задачи/напоминания, документы — всё проверено живьём на настоящей модели через
собственный релей на Render.com (обход гео-блокировки OpenRouter с российских IP).

**Дальше — Фаза 10, поддержка MAX** (сознательное расширение MVP за рамки
«Telegram-only» по прямому запросу пользователя, не пропущенная фаза). Код и тесты
готовы (86 тестов, все зелёные), не готова инфраструктура: нужен `MAX_BOT_TOKEN` от
пользователя (через `@MasterBot` в MAX), настройка nginx на сервере (переиспользовать
существующий бесплатный домен `159-194-214-72.sslip.io`, не трогая конфиг content-bot)
и регистрация вебхука — план по шагам в `DEPLOY.md`, раздел «MAX».
