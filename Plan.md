# Plan.md — рабочая приборная панель проекта Арина

## Правило поддержки актуальности плана (обязательно к исполнению)

После завершения каждой фазы, до начала следующей:
1. Отметить все задачи завершённой фазы статусом `✅ Готово` в таблице задач этой фазы.
2. Обновить строку фазы в «Таблице прогресса» ниже: **Факт** — что реально сделано и
   чем отличается от плана (если отличается), **Статус** → `✅`, **Блокеры** → очистить.
3. Отклонение от плана — фиксировать в столбце **Факт**, не переписывая **План** —
   так сохраняется история решений.
4. Заблокированная фаза — статус `🔴 Заблокировано`, в **Блокеры** — что мешает и что
   нужно, чтобы снять блокировку.
5. Полный технический план с архитектурными решениями и обоснованиями —
   `thoughts/shared/plans/2026-08-19-arina-mvp-implementation-plan.md` — не переписывать
   задним числом, только дополнять.

Источники: идея — `idea.md`, исследование — `research.md`, критика и фактчекинг —
`критика.md`, спецификация — `thoughts/shared/specs/2026-08-19-arina-personal-agent.md`,
технический план — `thoughts/shared/plans/2026-08-19-arina-mvp-implementation-plan.md`.

## Блокер верхнего уровня (снят)

Изначально здесь был зафиксирован блокер: разработка Фазы 3 и всего, что от неё
зависит (классификация, задачи, напоминания, summary диалога, документы — фактически
весь продукт, кроме каркаса), заблокирована до тех пор, пока в соседнем проекте
**Aiy** не стабилизирован в проде релей OpenRouter через Cloudflare Worker. До снятия
блокера допускалась только работа над Фазами 0-2.

**Снят 2026-08-19.** Решение: Арина подключается к OpenRouter напрямую, собственным
API-ключом, без релея и без зависимости от инфраструктуры Aiy. Причина — релей
технически не нужен: независимо проверено (из другой сессии), что
`https://openrouter.ai/api/v1/models` отвечает HTTP 200 напрямую с этой машины, без
прокси — геоблокировки нет. Фаза 3 разблокирована.

## Таблица прогресса

| Фаза | План | Факт | Статус | Блокеры |
|---|---|---|---|---|
| 0. Окружение и конвенции | Инициализировать репозиторий, `venv/`, `.gitignore`, `config.py` (pydantic-settings), `requirements.txt`, скелет структуры каталогов из `CLAUDE.md` | Сделано по плану: `git init`, `venv/`, `.gitignore` (venv/, __pycache__/, *.pyc, .env, *.db, *.sqlite3, кеши pytest/mypy), `requirements.txt` (стек из CLAUDE.md), `config.py` на pydantic-settings (`TELEGRAM_BOT_TOKEN`, `DATABASE_URL`, `OPENROUTER_BASE_URL`, `OPENROUTER_API_KEY`, `FERNET_KEY`, `ALLOWED_USER_IDS`), скелет каталогов `db/`, `llm/`, `core/`, `bot/handlers/`, `tests/calibration/` с `__init__.py`, `.env.example`, `tests/test_config.py` (3 теста, все зелёные) | ✅ | — |
| 1. Хранилище данных | `db/models.py` (User, Task, Note, DialogSummary), `db/crypto.py` (Fernet), `db/session.py`, Alembic-миграции, PostgreSQL локально | Сделано: `db/models.py` (User/Task/Note/DialogSummary, `context`/`status` как `Enum(..., native_enum=False)` — портируемо между Postgres и SQLite), `db/crypto.py` (`EncryptedString` — TypeDecorator, прозрачное Fernet-шифрование `profile_summary`/`Task.title`/`Note.content`/`DialogSummary.summary_text`), `db/session.py` (фабрика сессий из `settings.database_url`), Alembic настроен на `config.settings` и `Base.metadata`, начальная миграция автосгенерирована и вручную дополнена импортом `db.crypto`. Тесты: `tests/test_crypto.py`, `tests/test_models.py` (round-trip шифрования на `sqlite://` in-memory) — все зелёные. **Отклонение от плана**: в этом окружении нет локального PostgreSQL/Docker — `alembic upgrade head` реально проверен на SQLite (файл `arina_dev.db`, создан и удалён), а не на «чистой PostgreSQL» как в критерии готовности. Модели используют только портируемые типы, миграция должна быть перепроверена на настоящем PostgreSQL перед Фазой 9 (деплой). Также при попутной правке обнаружена и исправлена утечка состояния между тестами: `tests/test_config.py` мутировал `os.environ` на уровне модуля — переписан на `monkeypatch`. Docker Desktop 4.87.0 установлен через winget (2026-08-19). WSL2 включена (пользователь поставил вручную через `wsl --install` от администратора и перезагрузился — автоматическое повышение прав через `Start-Process -Verb RunAs` не сработало неинтерактивно, потребовалось сделать руками). Итог 2026-08-19: `docker compose up -d` поднял `postgres:16-alpine` (healthcheck зелёный), `DATABASE_URL` в `.env` переключён на `postgresql://arina:arina@localhost:5432/arina`, `alembic upgrade head` создал все 5 таблиц (`users`, `tasks`, `notes`, `dialog_summaries`, `alembic_version`) на настоящем PostgreSQL, вручную проверен round-trip шифрования через ORM (запись/чтение `Task.title`, `raw` — Fernet-шифротекст, расшифровка совпадает) — критерий готовности выполнен буквально. Попутно найдена и исправлена вторая утечка ambient-состояния: `test_allowed_user_ids_list_empty_by_default` неявно полагался на пустой `ALLOWED_USER_IDS` в `.env`, сломался после того как туда вписали реальные ID для Фазы 2 — переписан на явный `Settings(allowed_user_ids="")`. | ✅ | — |
| 2. Каркас Telegram-бота | `bot/main.py`, приём `/start`, онбординг-опрос (`bot/states.py`, `bot/handlers/onboarding.py`), whitelist приглашённых пользователей, идентификация по Telegram ID | Сделано: `bot/states.py` (`OnboardingState`), `bot/handlers/onboarding.py` (`/start` → whitelist-проверка по `ALLOWED_USER_IDS` → короткий опрос → `receive_profile` пишет `User.profile_summary` зашифрованным и ставит `onboarding_completed=True`; повторный `/start` уже онбордившегося — приветствие без повторного опроса), `bot/main.py` (`Application`, `ConversationHandler` на онбординг + `CommandHandler("delete_my_data", ...)`). `/delete_my_data` реализован сразу как в плане — каскадно удаляет Task/Note/DialogSummary и саму строку User. Тесты: `tests/test_onboarding.py` (7 тестов: отказ не-whitelisted, приветствие нового и вернувшегося пользователя, шифрование `profile_summary` на диске, удаление данных для существующего/несуществующего пользователя) — через мок `Update`/`SessionLocal` на SQLite in-memory, без реального Telegram. `python -m bot.main` собирает `Application` с плейсхолдер-токеном без сетевых обращений. Живая проверка пройдена 2026-08-19: реальный токен от @BotFather (`@arina_bf_bot`) получен, вписан в `.env`, бот поднят локально (`python -m bot.main`), реальный пользователь (Telegram ID из `ALLOWED_USER_IDS`) прошёл `/start` → онбординг-вопрос → ответил текстом → получил «Профиль сохранён»; в БД (`arina_dev.db`) строка `User` с `onboarding_completed=True`, `profile_summary` расшифровывается ORM-слоем в исходный текст, а сырое значение колонки — Fernet-шифротекст (`gAAAAAB...`), не открытый текст. Второй ID из whitelist ещё не проходил онбординг на момент проверки — это не блокирует критерий, один успешный проход подтверждает работоспособность. Попутно найдено и объяснено (не баг): `venv`-лаунчер `python.exe` на этой машине запускает системный `python.exe` как дочерний процесс с тем же argv — выглядит как дублирующийся поллер, но функционально безвреден (один процесс делает всю работу). | ✅ | — |
| 3. LLM-ядро | `llm/client.py` (OpenRouter через релей, retry), `llm/classify.py` (работа/личное), калибровочный набор диалогов, `tests/calibration/` | **Отклонение от плана**: релей OpenRouter через Cloudflare Worker (Aiy) не используется — 2026-08-19 принято решение подключаться к OpenRouter напрямую, собственным API-ключом. Причина: релей технически не нужен — независимо проверено (из другой сессии), что `https://openrouter.ai/api/v1/models` отвечает HTTP 200 напрямую с этой машины, без прокси, геоблокировки нет. `llm/client.py` использует `base_url=https://openrouter.ai/api/v1` вместо адреса Cloudflare Worker. Блокер верхнего уровня снят (см. раздел выше). Сделано: `llm/client.py` (`complete()` — обёртка над `openai.AsyncOpenAI`, retry с экспоненциальной паузой на `APIConnectionError`/`APITimeoutError`, `LLMUnavailableError` с понятным русским текстом после исчерпания попыток), `llm/classify.py` (`classify_message()` — промпт классификации на `Context.work`/`Context.personal` с уверенностью, разбор JSON устойчив к markdown-обёртке в ответе модели). Тесты: `tests/test_llm_client.py` (успех, ретрай-затем-успех, исчерпание попыток — на моках, без реальной сети), `tests/test_classify.py` (парсинг чистого и обёрнутого в markdown JSON, ошибка на нераспознаваемом ответе). `tests/calibration/dataset.py` — 10 сообщений (явно рабочие/личные + 2 неоднозначных, включая «созвон с Димой в 19» из спецификации), `tests/calibration/test_calibration.py` — пропускается по умолчанию (`RUN_CALIBRATION=1` для реального запуска), порог точности 0.7 временный, обсуждается по факту первых прогонов. Модель по умолчанию — `anthropic/claude-haiku-4.5` (не зафиксирована в исходных документах проекта, выбрана как быстрая/дешёвая для задачи бинарной классификации — решение этой сессии, может быть пересмотрено). | 🟡 | Нужен настоящий `OPENROUTER_API_KEY` (сейчас в `.env` dev-плейсхолдер) для живой проверки клиента и прогона калибровочного набора |
| 4. Свободный чат + классификация | `bot/handlers/free_chat.py` — приём сообщений, классификация, инлайн-кнопка коррекции классификации | — | ⬜ Не начато | Зависит от Фазы 3 |
| 5. Задачи и напоминания | `core/tasks.py` (разбор естественного языка), `core/scheduler.py` (APScheduler), `bot/handlers/tasks_flow.py`, разовые + повторяющиеся напоминания | — | ⬜ Не начато | Зависит от Фазы 3 |
| 6. Память и summary диалога | `core/dialog_summary.py` — скользящее текстовое summary, окно/частота пересуммаризации | — | ⬜ Не начато | Зависит от Фазы 3 |
| 7. Документы/письма | `llm/documents.py`, `bot/handlers/documents_flow.py` — генерация с нуля + явное подтверждение перед использованием | — | ⬜ Не начато | Зависит от Фазы 3 |
| 8. Безопасность и 152-ФЗ | Шифрование во всех точках записи, команда полного удаления данных (`/delete_my_data`), проверка отсутствия PII в логах | Частично сделано попутно с Фазой 2 (`/delete_my_data` уже реализован — см. Фазу 2): проверено `grep` по всему проекту — логирование нигде не настроено и текст сообщений не выводится ни в print, ни в logging. В `bot/main.py` добавлена базовая `logging.basicConfig` (только технические события уровня INFO — какой хендлер сработал/ошибки, без текста сообщений пользователя). Дальнейшая часть (полная проверка логов после Фаз 4-7, когда появится LLM-логика) остаётся не начатой. | 🟡 | Основная часть зависит от Фаз 1 (Postgres), 4-7 (LLM-логика ещё не написана) |
| 9. Закрытая бета | Список приглашённых, деплой на Beget, ручной прогон сценариев, запуск замера retention (1/2/4 недели) | — | ⬜ Не начато | Зависит от всех предыдущих фаз |

Статусы: ⬜ Не начато · 🟡 В работе · ✅ Готово · 🔴 Заблокировано

## Следующее конкретное действие

Фазы 0, 1 и 2 закрыты на `✅` (2026-08-19). Локальное окружение полностью рабочее:
Postgres в Docker (`docker compose up -d`), реальный Telegram-бот `@arina_bf_bot`
проходит живой онбординг, шифрование подтверждено на настоящем PostgreSQL.

Блокер верхнего уровня снят (2026-08-19, прямое подключение к OpenRouter вместо
релея Aiy) — Фаза 3 разблокирована. Функционально готова: `llm/client.py`,
`llm/classify.py`, калибровочный набор — 19 тестов зелёные, статус `🟡` (не `✅`):
нужен настоящий `OPENROUTER_API_KEY` вместо dev-плейсхолдера в `.env` (получить на
openrouter.ai) для живой проверки `complete()` и реального прогона калибровки
(`RUN_CALIBRATION=1 pytest tests/calibration`), чтобы замерить точность и осознанно
зафиксировать порог. Когда ключ появится — вписать в `.env`, прогнать калибровку,
закрыть Фазу 3 на `✅`, переходить к Фазе 4 (свободный чат + классификация).
